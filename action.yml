name: 'PR Assign Reviewers'
description: 'Assign user(s) or team(s) as PR reviewers'
inputs:
  github-token:  # id of input
    description: 'GitHub token'
    required: true
  reviewers:  # id of input
    description: 'Reviewers users'
    required: true
  team-reviewers:  # id of input
    description: 'Reviewers teams'
    required: true
runs:
  using: 'node16'
  main: 'index.js'